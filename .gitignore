# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
out/
.next/
.turbo/
*.tsbuildinfo

# foundry
cache/
broadcast/*/*/dry-run/

# environment & secrets
.env
.env.*
!.env.example
*.pem
*.key
mnemonic.txt

# ML models — large, fetched not committed
*.onnx
models/

# test fixtures output
fixtures/generated/
coverage/

# os / editor
.DS_Store
.vscode/
.idea/

# internal planning — kept local, never published
# competitor survey, bounty strategy, judge notes, cut list
RESEARCH_NOTES.md
BUILD_PLAN.md

# TrustMark binary and ONNX models -- ~95 MB, rebuilt by scripts/setup-trustmark.sh
.tools/

# Screenshot fixtures are NOT published: they contain other projects of the
# author's (stellarinsure, Spectra, conduit-protocol) and machine hostname.
# They stay local so the robustness matrix still runs over all 16 fixtures.
fixtures/sources/screenshot-*.png

# Seed corpus: fetched images and seeding state, neither redistributed
.corpus/
.corpus-seeded.json

# Rust build artifacts
target/
